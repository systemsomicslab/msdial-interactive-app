[project]
name = "msdial-interactive-app"
version = "0.1.0"
description = "Cross-platform interactive workflow assistant for MS-DIAL Console"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
msdial-interactive = "msdial_app.server:main"

[tool.setuptools]
packages = ["msdial_app"]

